version: "3.9"
services:
  node-backend:
    image: ghcr.io/nmemmert/auth-node-backend:latest
    pull_policy: always
    restart: unless-stopped
    environment:
      PORT: ${NODE_BACKEND_PORT:-4100}
      AUTHELIA_USERS_FILE: /data/users_database.yml
      ADMIN_API_TOKEN: ${ADMIN_API_TOKEN}
    volumes:
      - ${AUTHELIA_USERS_FILE_HOST}:/data/users_database.yml
    ports:
      - "${NODE_BACKEND_PORT:-4100}:${NODE_BACKEND_PORT:-4100}"

  node-frontend:
    image: ghcr.io/nmemmert/auth-node-frontend:latest
    pull_policy: always
    restart: unless-stopped
    environment:
      NODE_BACKEND_PORT: ${NODE_BACKEND_PORT:-4100}
    depends_on:
      - node-backend
    ports:
      - "${NODE_FRONTEND_PORT:-3100}:3100"

  python-backend:
    image: ghcr.io/nmemmert/auth-python-backend:latest
    pull_policy: always
    restart: unless-stopped
    environment:
      PORT: ${PYTHON_BACKEND_PORT:-5100}
      AUTHELIA_USERS_FILE: /data/users_database.yml
      ADMIN_API_TOKEN: ${ADMIN_API_TOKEN}
    volumes:
      - ${AUTHELIA_USERS_FILE_HOST}:/data/users_database.yml
    ports:
      - "${PYTHON_BACKEND_PORT:-5100}:${PYTHON_BACKEND_PORT:-5100}"

  python-frontend:
    image: ghcr.io/nmemmert/auth-python-frontend:latest
    pull_policy: always
    restart: unless-stopped
    depends_on:
      - python-backend
    ports:
      - "${PYTHON_FRONTEND_PORT:-3200}:3200"
